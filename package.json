{
  "name": "ai-game-collection",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix portal run dev",
    "build": "npm --prefix portal run build",
    "test": "npm --prefix portal test"
  }
}
